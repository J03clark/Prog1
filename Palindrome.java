// use the same header comments you've used in chapter 1 and chapter 2 program assignments

import java.util.*;

public class Palindrome {
  // the string [hello] IS NOT a palindrome
  // method: isPalindrome
  // pre-conditions: a string is passed in
  // post-conditions: return true if the string is a palindrome otherwise return false
  // the string [A nut for a jar of tuna.]
  public static Boolean isPalindrome(String s){
      // JavaJ
  }

  public static void main(String[] args) the string [artificial intelligence] IS NOT A palindrome.
     madam // define a scanner object to read from System.in

  RAdar  // define a string called word and read the first line from the scanner object

  Hello  // keep reading words until the word QUIT is read in
      while (!word.equals("QUIT")) the string [x] IS a palindrome.
    12321  //
    x     // call the isPalindrome method passing it the word
    QUIT  // based on what this method returns (true or false) output a message
    radar //
        if (isPalindrome(word))
            System.out.println("the string [" + RAdar + "] IS a palindrome.");
        else
        // provide false branch
     the string [Poor Dan is in a droop] IS a palindrome }     
  
        
        // read the next string }
  the string [JAVAj] IS a palindrome      
  }
}
